import system'routines.
import extensions.

public program =
[
    console writeLine(console readLine;
                              split;
                              selectBy(%"convertorOp.toInt[0]");
                              summarize).
].
