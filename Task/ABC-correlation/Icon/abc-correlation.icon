procedure main()
   writes("Enter a string: ")
   write(if ABC(read()) then "True" else "False")
end

procedure ABC(s)
   a := b := c := 0
   every l := !s do {
      case l of {
         "a": a +:= 1
         "b": b +:= 1
         "c": c +:= 1
         }
      }
    return a = b = c
end
