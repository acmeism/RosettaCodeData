!ys-0

defn main():
  each word filter(abc-word? words()):
    say: word

defn abc-word?(word):
  word.index('a').?? &&
  word.index('b').?? &&
  word.index('c').??

defn words():
  file =: '/usr/share/dict/words'
  if fs/e(file):
    then: file:read:lines.map(\(_:lc))
    else: qw(abacus abecedarian alphabetic backscatter cab)
