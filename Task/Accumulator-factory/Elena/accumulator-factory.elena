function =
    (:acc)((:n)(acc append:n)).

accumulator =
    (:n)(function(Variable new(n))).

public program =
[
    var x := accumulator(1).

    x(5).

    var y := accumulator(3).

    console write(x(2.3r)).
].
