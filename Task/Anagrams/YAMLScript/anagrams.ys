#!/usr/bin/env ys-0

defn main(file='/tmp/unixdict.txt'):
  words =: read(file):lines
  groups =: group-by(\(_:sort:join) words)
  max =: groups:vals.map(count).apply(max)
  each group groups:vals:
    when group.# == max:
      say: group:joins
