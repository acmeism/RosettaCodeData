procedure main(A)
   limit := \A[1] | 20
   n := 0
   every (n <:= *factors(f := seq()))\limit do writes(" ",f)
   write()
end

procedure factors(n)  # Note this is all factors, not just prime factors
   every (f := [], i := 1 to n) do
      if n%i = 0 then put(f,i)
   return f
end
