local fmt = require "fmt"

local list1 = range(1, 9)
local list2 = range(10, 18)
local list3 = range(19, 27)

local list = {}
for i = 1, 9 do
    list[i] = tostring(list1[i]) .. tostring(list2[i]) .. tostring(list3[i])
end
fmt.lprint(list)
