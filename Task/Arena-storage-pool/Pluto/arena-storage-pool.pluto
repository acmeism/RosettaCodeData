local arena = table.create(5)     -- allocate memory for 5 values
for i = 1, 5 do arena[i] = i end  -- insert some integers
print(arena:concat(", "))         -- print them
arena = nil                       -- make arena eligible for garbage collection
collectgarbage()                  -- request immediate garbage collection
