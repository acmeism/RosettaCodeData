import complex
var a = complex(1.0,1.0)
var b = complex(3.1415,1.2)

echo("a    : " & $a)
echo("b    : " & $b)
echo("a + b: " & $(a + b))
echo("a * b: " & $(a * b))
echo("1/a  : " & $(1.0/a))
echo("-a   : " & $(-a))
