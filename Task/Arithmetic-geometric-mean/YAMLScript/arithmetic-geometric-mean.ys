!ys-0

defn main(a=1, g=nil):
  g ||=: 1 / sqrt(2)
  say: "agm($a $g) = $(agm(a g))"

defn agm(a g):
  loop a a, g g:
    if (a - g):abs >= 1.0e-15:
      recur:
        ((a + g) / 2)
        Math/sqrt(a * g)
      else: a
