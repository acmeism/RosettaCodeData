list
ac(list a, list b)
{
    list o;

    o.copy(a);
    b.ucall(l_append, 1, o);

    o;
}

integer
main(void)
{
    list a, b, c;

    a = list(1, 2, 3, 4);
    b = list(5, 6, 7, 8);

    c = ac(a, b);

    c.ucall(o_, 1, " ");

    return 0;
}
