#define std'routines'arrays'*.

#symbol Program =
[
    #var a := (1,2,3).
    #var b := (4,5).

    #var c := a~earrayop add:b.

    'program'output << "(" << a << ") + (" << b << ") = (" << c <<")%n".
].
