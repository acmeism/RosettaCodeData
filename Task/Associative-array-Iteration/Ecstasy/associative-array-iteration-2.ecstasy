module AssociativeArrayIteration {
    void run() {
        @Inject Console console;
        val map = ["hello"=7, "world"=101];

        map.entries.forEach(console.print); // print all key/value pairs
        map.keys.forEach(console.print);    // print all keys
        map.values.forEach(console.print);  // print all values
    }}
