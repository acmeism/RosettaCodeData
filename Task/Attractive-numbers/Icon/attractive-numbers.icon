link factors

procedure main(A)
   limit := \A[1] | 120
   every writes(" ",attractive(seq()\limit)|"\n")
end

procedure attractive(n)
   every (cnt := 0) +:= (genfactors(n),1) | return (isprime(cnt),n)
end
