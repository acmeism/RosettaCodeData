real
mean(list l)
{
    integer i;
    real sum, x;

    sum = 0;
    for (i, x in l) {
        sum += x;
    }

    return sum / ~l;
}

integer
main(void)
{
    o_form("%f\n", mean(l_effect(4.5, 7.25, 5r, 5.75)));

    return 0;
}
