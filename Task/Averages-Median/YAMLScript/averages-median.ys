#!/usr/bin/env ys-0

defn main(*nums):
  nums |||=: +[4.1 5.6 7.2 1.7 9.3 4.4 3.2]
  say: "Median: $(median(nums))"

defn median(xs):
  sorted =: sort(xs)
  n =: sorted.#
  mid =: n / 2
  if (n % 2):zero?:
    then: (sorted.nth(mid.--) + sorted.$mid) / 2.0
    else: sorted.$mid
