!ys-0

defn main(*args):
  nums =:
    if args:
      args.map(N)
      [1 2 3 4 5 6 6 7 8 9 10 10 10]
  say: mode(nums):joins

defn- mode(coll):
  freqs =: coll:frequencies
  max-freq =: apply(max freqs:vals)
  sort:
    freqs.filter(\(_.1 == max-freq)).map(first)
