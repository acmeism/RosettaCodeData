get "libhdr"

let nondec(x) =
    x = 0 -> false,
    #XA <= (x & #XF) <= #XF -> true,
    nondec(x >> 4)

let start() be
$(  let c = 0
    for n=1 to 500
        if nondec(n)
        $(  writed(n,4)
            c := c + 1
            if c rem 20=0 then wrch('*N')
        $)
    wrch('*N')
$)
