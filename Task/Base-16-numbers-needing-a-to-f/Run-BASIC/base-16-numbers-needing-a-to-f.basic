for i = 1 to 500
  if needsaf(i) then print i; " ";
next i
end

function needsaf(n)
  while n > 0
    if (n mod 16) > 9 then needsaf = 1 : goto [exit]
    n = int(n / 16)
  wend
  needsaf = 0
[exit]
end function
