sub needs_af (n)
    while n > 0
        if mod(n, 16) > 9 then return true : fi
        n = int(n / 16)
    wend
    return false
end sub

for i = 1 to 500
    if needs_af(i) then print i, " ", : fi
next i
end
