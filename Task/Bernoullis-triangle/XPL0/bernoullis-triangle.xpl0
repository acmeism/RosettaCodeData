int  Row(15, 15), N, K;
[for N:= 0 to 15-1 do
    [Row(N, 0):= 1;
    for K:= 1 to N-1 do
        Row(N, K):= Row(N-1, K-1) + Row(N-1, K);
    Row(N, K):= Row(N, K-1) + 1;
    for K:= 0 to N do
        [IntOut(0, Row(N, K));  ChOut(0, ^ )];
    CrLf(0);
    ];
]
