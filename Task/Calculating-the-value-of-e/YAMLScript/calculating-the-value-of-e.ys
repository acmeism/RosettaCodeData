#!/usr/bin/env ys-0

defn main(epsilon=1.0e-15):
  say: e(epsilon)

defn e(eps):
  loop n 0, term 1.0, sum 0.0:
    if term < eps:
      then: sum
      else:
        recur n.++ (term / n.++): sum + term
