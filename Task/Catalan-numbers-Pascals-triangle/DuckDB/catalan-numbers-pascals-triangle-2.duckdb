create or replace function catalan_by_pascal(n) as (
  nCr(2*n, n) - nCr(2*n, n-1)
);

# Illustration
select n, catalan_by_pascal(n::UHUGEINT) from (
  from range(1,16) _(n) union all from range(60,63)
) order by n;
