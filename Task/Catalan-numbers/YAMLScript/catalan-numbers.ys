!ys-0

defn main(n=15):
  each i (0 .. n):
    say: "C($i) = $catalan(i)"

# Direct formula: C(n) = (2n)! / ((n+1)! * n!)
defn catalan(n):
  factorial(2 * n) / (factorial(n.++) * factorial(n))

defn factorial(n):
  if n.?:
    reduce mul: bigint(1) (1 .. n)
    else: 1
