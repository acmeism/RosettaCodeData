local a = {1, 2, 3, 4, 5}
local sum    = a:reduce(|acc, i| -> acc + i)
local prod   = a:reduce(|acc, i| -> acc * i, 1)
local sum_sq = a:reduce(|acc, i| -> acc + i * i)
print("{" .. a:concat(", ") .. "}")
print($"Sum is {sum}")
print($"Product is {prod}")
print($"Sum of squares is {sum_sq}")
