local ffi = require "pluto:ffi"
local lib = ffi.open(os.platform == "macos" ? "libSystem.B.dylib" : "libc.so.6")
lib:cdef[[ int isatty(int fd); ]]

local res = lib.isatty(1)
print($"Output device is a terminal? {res == 1}")
