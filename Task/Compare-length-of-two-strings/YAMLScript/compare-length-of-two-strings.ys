!ys-0

# Print strings sorted by length, longest first, with the length
# alongside each. Stable sort keeps input order for ties.

defn main():
  strs =: qw(abcd 123456789 abcdef 1234567)
  sorted =: sort-by(\(- _:len) strs)
  each s sorted:
    say: "$(s.#)  $s"
