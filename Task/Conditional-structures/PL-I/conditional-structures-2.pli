select (i); /* select on value of variable */
  when (1,4,9)
    do;
      statement(s)
    end;

  when (11, 42)
    do;
      statement(s)
    end;

  other /* everything else */
    do;
      statement(s)
    end;
end;
