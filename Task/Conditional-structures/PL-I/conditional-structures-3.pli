select (i); /* select on value of variable */
  when (1,4,9)
    do;
      statement_s;
    end;

  when (11, 42)
    do;
      statement_s;
    end;

  other /* everything else */
    do;
      statement_s;
    end;
end;
