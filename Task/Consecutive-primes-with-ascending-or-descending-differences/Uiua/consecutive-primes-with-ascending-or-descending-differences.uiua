# Find strictly increasing (decreasing) diffs in primes.

Ps ← ↘1▽⊸=⊣⊸°/×↘1⇡1000000
# Find length of max run, find starts, return runs.
MaxRun ← ⊏≡⌞+⇡+2 ⟜(⊚⌕˜↯1) ⊸(/↥˙⊜⧻)

∩⊢∩⌟MaxRun ⊃⧈<⧈> ⊸⧈- Ps
