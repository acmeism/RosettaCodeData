create or replace function countSubstring(string, substring) as (
  regexp_extract_all(regexp_escape(string), substring)
);

select s, ss, length(countSubstring(s,ss)) as count
  from read_csv('string-substring.csv', header=false, sep='\t') t(s,ss);
