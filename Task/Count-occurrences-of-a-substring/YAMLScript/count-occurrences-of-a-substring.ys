!ys-0

defn main(text='the three truths' sub='th'):
  say: count-sub(text sub)

defn count-sub(s sub):
  loop i 0, n 0:
    j =: str/index-of(s sub i)
    if j:
      recur: (j + sub.#) n.++
      else: n
