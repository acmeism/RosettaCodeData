!ys-0

defn main(limit=100):
  each p (2 .. limit):
    when prime?(p) && prime?(p + 4):
      say: "$p $(p + 4)"

defn prime?(n):
  condp gt n:
    2: false
    4: true
    else:
      every? _ (2 .. n:sqrt):
        fn(d): (n % d).?
