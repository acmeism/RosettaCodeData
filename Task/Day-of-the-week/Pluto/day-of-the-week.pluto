local fmt = require "fmt"

print("Years between 2008 and 2121 when 25th December falls on Sunday:")
local years = {}
for year = 2008, 2121 do
    local day = os.date("%A", os.time({year = year, month = 12, day = 25}))
    if day == "Sunday" then years:insert(year) end
end
fmt.lprint(years)
