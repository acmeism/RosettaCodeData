#!/usr/bin/env ys-0

defn main():
  say: '--police--  --sanitation--  --fire--'
  each p (2 .. 6), s (1 .. 7), f (1 .. 7):
    when valid?(p s f):
      say: "    $p           $s           $f"

defn valid?(p s f):
  and:
    p:even? s.ne(p) f.ne(p) f.ne(s)
    (p + s + f).eq(12)
