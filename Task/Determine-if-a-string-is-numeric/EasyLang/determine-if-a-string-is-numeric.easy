proc is_numeric a$ . r .
  h = number a$
  r = 1 - error
  # because every variable must be used
  h = h
.
for s$ in [ "abc" "21a" "1234" "-13" "7.65" ]
  call is_numeric s$ r
  if r = 1
    print s$ & " is numeric"
  else
    print s$ & " is not numeric"
  .
.
