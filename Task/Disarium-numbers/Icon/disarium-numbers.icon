procedure main(A)
   limit := \A[1] | 18
   every writes(" ",disarium(seq(0)))\limit
   write()
end

procedure disarium(n)
   ns := string(n)  # Avoid redundant conversions later
   every ((s := i := 0), j := !ns, i +:= 1) do s +:= ns[i]^i
   return s = n
end
