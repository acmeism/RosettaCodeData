func mdr(n, base=10) {
    while (n >= base) {
        n = n.digits(base).prod
    }
    return n
}

func is_dividuus(n, base=10) {
    [{.digits_sum(base)}, {.digits(base).prod}, {.digital_root(base)}, {mdr(_, base)}].all {|f|
        f(n) `divides` n
    }
}

say 50.by(is_dividuus)
