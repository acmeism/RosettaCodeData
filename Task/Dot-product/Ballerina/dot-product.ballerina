import ballerina/io;

function dot(float[] a, float[] b) returns float {
    float sum = 0.0;
    foreach any[] [idx, val] in a.enumerate() {
        sum += val * b[idx];
    }
    return sum;
}

public function main() {
    io:println(dot([1,  3, -5], [4, -2, -1]));
}
