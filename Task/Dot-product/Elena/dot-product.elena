import extensions.
import system'routines.

extension $op
{
    method dotProduct array:array
        = self zip(array) by(:x:y)(x * y); summarize.
}

program =
[
    console printLine((1, 3, -5) dotProduct:(4, -2, -1)).
].
