(fn dotProduct [v1 v2]
    (assert (= (length v1) (length v2)) "vectors should have the same length")

    (var result 0)
    (for [i 1 (length v1)]
         (set result (+ result (* (. v1 i) (. v2 i))))
    )
    result
)

(print (dotProduct [1 3 -5] [4 -2 -1]))
