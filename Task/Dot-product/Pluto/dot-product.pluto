local a = {1, 3, -5}
local b = {4, -2,-1}

function table.dot(v, w)
    assert(#v == #w, "dot: vectors lengths must be same")
    local sum = 0
    for i = 1, #a do
        sum += a[i] * b[i]
    end
    return sum
end

print(a:dot(b))
