fn dot_product(a: Vec<int>, b: Vec<int>) -> int {
    let mut s = 0;
    for i in range(0, a.len()) {
        s += a[i] * b[i];
    }
    return s;
}

fn main() {
    let v1 = vec!(1, 3, -5);
    let v2 = vec!(4, -2, -1);

    println!("{}", dot_product(v1, v2));
}
