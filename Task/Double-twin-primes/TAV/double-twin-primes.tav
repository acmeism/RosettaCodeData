main (parms):+
    lim =: string parms[1] as integer else 1000
    ?# i =: from 3 upto lim step 2
        ? integer i factor = ()     \ in the standard library
            ? integer i+2 factor = ()
                ? integer i+6 factor = ()
                    ? integer i+8 factor = ()
                        print i, i+2, i+6, i+8
