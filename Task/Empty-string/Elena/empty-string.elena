#import system.
#import extensions.

#symbol program = [
    #var s := emptyLiteralValue.

    (s is &empty)
        ? [ console writeLine:"'":s:"' is empty". ].

    (s is &nonempty)
        ? [ console writeLine:"'":s:"' is not empty". ].
].
