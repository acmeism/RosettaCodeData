import extensions.

program = [
    var s := emptyLiteralValue.

    if (s isEmpty)
        [ console printLine("'", s, "' is empty") ].

    if (s isNonempty)
        [ console printLine("'", s, "' is not empty") ]
].
