local s = io.contents(arg[0]):strip()
local m = {}
for i = 1, #s do
    local c = s[i]
    local d = m[c]
    m[c] = d ? d + 1 : 1
end
local hm = 0
for m:keys() as k do
    local c = m[k]
    hm += c * math.log(c, 2)
end
local l = #s
print(math.log(l, 2) - hm / l)
