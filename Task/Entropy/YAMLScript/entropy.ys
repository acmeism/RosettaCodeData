!ys-0

defn main(s='1223334444'):
  n =: s.#
  log2 =: Math/log(2)
  entropy =: sum(s:frequencies:vals.map(\(entropy-term(_ n log2))))
  say: "$format('%.5f' entropy) bits/symbol"

defn- entropy-term(cnt n log2):
  p =: F(cnt) / n
  term =: p * (Math/log(p) / log2)
  0 -: term
