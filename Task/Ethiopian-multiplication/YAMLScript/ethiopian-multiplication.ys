!ys-0

defn main(a=17 b=34):
  say: "$a * $b = $(ethiopian(a b))"

defn ethiopian(a b):
  loop x a, y b, sum 0:
    if x == 0:
      then: sum
      else:
        new-sum =:
          if x:odd?:
            (sum + y)
            sum
        recur: x.quot(2) (y * 2) new-sum
