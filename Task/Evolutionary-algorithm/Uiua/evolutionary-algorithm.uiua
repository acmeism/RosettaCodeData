T    ← "METHINKS@IT@IS@LIKE@A@WEASEL"
N    ← 1                    # Better than any larger number.
C    ← 5                    # As fast as any larger number.
L    ← (+@@×27⚂)            # Random A-Z (or @ for space).
R    ← ⍥L⧻T                 # Random string.
F    ← /+≠ T                # Fitness. Number of wrong chars. Higher is worse.
M    ← ∧⍜⊡⋅L⊃(˜↙⍏⍥⚂⧻|∘)⊃⋅∘∘ # Mutate `a` random chars of `b`.
Best ← ⊢▽=⊸/↧⊸≡F ⍥(⊸M N)C   # Choose best of C mutations of N chars.
⍢(⊙+₁Best|>0F) R 0          # Start with random string, mutate until perfect.
