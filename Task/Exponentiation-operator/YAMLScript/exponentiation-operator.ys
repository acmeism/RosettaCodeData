!ys-0

defn main(base=2 exp=10):
  say: base.power(exp)
  say: base:F.power(exp)

defn power(base exp):
  loop acc 1, e exp:
    if e:zero?:
      then: acc
      else:
        recur: (acc * base) e.--
