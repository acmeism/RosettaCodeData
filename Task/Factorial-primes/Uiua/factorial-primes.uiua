# Factorial primes

IsP ← =1⧻⍣°/×[]
F‼  ← ≡⍜°□₂(⊓($"_ _"⊙^1|^0)) # Apply and record +/- 1.

≡(⊟∩□$"_!"°⊟)⍉⊟⟜\×↘1⇡15              # Get base factorials.
▽⊸≡(IsP°□⊣)⊂⊃(F‼+₁ "+ 1"|F‼-₁ "- 1") # +/- 1. Keep primes.
≡(&p$"_\t = _"°□₂)⊏⍏⊸≡(°□⊣)          # Sort and print.
