defun factorial-rec(n):
  if n < 0: error("n was expected to be non-negative, but it is ~d", n)
  elif n < 2: n
  else: n * factorial-rec(n - 1)
  end
end

# shorter version: no negative input check
defun fac(n):
  ifelse (n < 2) n (n * fac(n - 1))
end
