PROC main:
  LOCAL n%,na%,i%
  WHILE n%=0
    PRINT "Please enter an integer."
    INPUT n%
  ENDWH
  na%=ABS(n%)
  DO
    i%=i%+1
    IF na%=INT(na%/i%)*i%
      PRINT i%,
    ENDIF
  UNTIL i%=INT(na%/2)
  PRINT na%
  GET
ENDP
