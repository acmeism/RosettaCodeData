use std::fs;

fn main() {
    let metadata = match fs::metadata("file.txt") {
        Err(e) => {
                println!("Error: {}", e);
                return;
            },
        Ok(f) => f,
    };

    println!("Size of file.txt is {}", metadata.len());
}
