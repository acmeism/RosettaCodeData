squarep(n) := integerp(sqrt(n))$
square_diff(p) := block(diff: next_prime(p)-p, squarep(diff) and diff>36)$

sq_primes: sublist(primes(2, 1000000), square_diff)$
for p in sq_primes do(
    q: next_prime(p),
    sprint(q, "-", p, "=", q-p),
    newline()
);
