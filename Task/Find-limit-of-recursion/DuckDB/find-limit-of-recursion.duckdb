create or replace function r(mx) as (
  with recursive cte as (
    select 0::UHUGEINT as i
    union all
    select i+1 as i
    from cte
    where i < mx)
  select last(i) from cte
);

select r(1000000);
