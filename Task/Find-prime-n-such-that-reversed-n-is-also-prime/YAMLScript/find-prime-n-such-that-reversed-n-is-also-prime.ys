!ys-0

defn main(limit=500):
  say:
    (1 .. limit.--).filter(\(prime?(_) &&
    prime?(reverse-num(_)))):joins

defn reverse-num(n):
  n:S:reverse:I

defn prime?(n):
  when n >= 2:
    if n >= 4:
      not: (2 .. sqrt(n):I)
        .some(\((n % _).!))
      else: true
