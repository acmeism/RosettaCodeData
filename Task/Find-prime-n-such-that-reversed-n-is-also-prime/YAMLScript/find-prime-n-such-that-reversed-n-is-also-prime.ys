!ys-0

defn main(limit=500):
  say:
    (1 .. limit.--).filter(\(prime?(_) &&
    prime?(reverse-num(_)))):joins

defn reverse-num(n):
  n:S:reverse:I

defn prime?(n):
  (n > 1) &&
  range(2 n:sqrt:I.++)
  .every?(\((n % _).?))
