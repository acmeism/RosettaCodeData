local int = require "int"
local fmt = require "fmt"

local limit = 200
for n = 1, limit - 1, 2 do
    local p = n * n * n + 2
    if int.isprime(p) then
        fmt.print("n = %3d => n³ + 2 = %,9s", n, p)
    end
end
