!ys-0

defn main(limit=200):
  say: (1 .. limit.--)
    .map(\(_:cube + 2))
    .filter(prime?):joins

defn prime?(n):
  when n >= 2:
    if n >= 4:
      not: (2 .. sqrt(n))
        .some(\((n % _).!))
      else: true
