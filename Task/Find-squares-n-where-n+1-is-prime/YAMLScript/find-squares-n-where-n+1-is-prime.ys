!ys-0

defn main(limit=1000):
  say: (1 .. sqrt(limit):I).map(sqr).filter(\(prime?(_.++))):joins

defn prime?(n):
  when n >= 2:
    if n >= 4:
      not: (2 .. sqrt(n)).some(\((n % _).!))
      else: true
