!ys-0

defn main(*):
  A =: +[math/sin math/cos cube]
  B =: +[math/asin math/acos cbrt]
  say: "Composing each f with its inverse at x=0.5:"
  each i (0 .. A.#.--):
    f =: A.$i
    g =: B.$i
    result =: g(f(0.5))
    say: "g(f(0.5)) = $result"

defn cube(x): x * x * x

defn cbrt(x): x ** 1/3
