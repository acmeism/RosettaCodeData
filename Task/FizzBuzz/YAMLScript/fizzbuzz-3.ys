map(say):
  loop [i 1, l []]:
    if (i > 100):
    - l
    - ^^^:
      - inc(i)
      - conj:
        - l
        - ???:
            zero?(mod(i 15)) : "FizzBuzz"
            zero?(mod(i 5))  : "Buzz"
            zero?(mod(i 3))  : "Fizz"
            :else: i
