local fmt = require "fmt"

local function floyd(n)
   local k = 1
   for i = 1, n do
       for j = 1, i do
           local l = (j < 9) ? 2 : 3
           fmt.write($"%{l}d ", k)
           k += 1
       end
       print()
   end
end

print("Floyd(5):")
floyd(5)
print("\nFloyd(14):")
floyd(14)
