Floyd ← ⊜□⇌/(⊂↯.:)⟜(+1⇡/+)+1⇡
Print ← (
  ≡(□≡(°⋕))            # Stringify terms.
  ≡(⊢⬚""↯⊟∞:°□)⟜(◇/↥⧻) # Pad rows with "" to same dims.
  ≡⊟¤+1≡◇⧻⊡¯1.         # Find pads and couple with each.
  ≡(
    ≡(□/◇⊂↙¯⟜(⊂↯⊙@ )∩°□°⊟)⍉ # Prepad each term.
    &p/⍚⊂                   # Join each row and print.
  )
)
Print Floyd 4
&p""
Print Floyd 14
