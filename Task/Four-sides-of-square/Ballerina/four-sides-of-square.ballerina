import ballerina/io;

function hollowMatrix(int n) {
    foreach int i in 0..<n {
        foreach int j in 0..<n {
            io:print((i == 0 || i == n-1 || j == 0 || j == n-1) ? "1 " : "0 ");
        }
        io:println();
    }
}

public function main() {
    hollowMatrix(9);
}
