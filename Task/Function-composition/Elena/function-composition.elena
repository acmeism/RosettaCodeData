import extensions.

func1 extension op
{
    compose(BaseFunction1 f)
        = (:x)(self eval(f eval:x)).
}

program =
[
    var fg := (:x)(x + 1) compose(:x)(x * x).

    console printLine(fg eval(3)).
].
