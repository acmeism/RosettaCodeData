in-package :moonli-user

mapcar(
  lm (x,y): x + y,
  (1,2,3),
  (4,5,6)
) #=> (5,7,9)

mapcar(
  lambda (x,y):
    concatenate(
      $string,
      write-to-string(x),
      write-to-string(y)
    )
  end,
  (1,2,3),
  (4,5,6)
) #=> ("14", "25", "36")

