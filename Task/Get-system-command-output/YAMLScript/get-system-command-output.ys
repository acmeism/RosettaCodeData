!ys-0

# Run uname -a via the built-in sh helper and split its output
# into lines for any downstream processing.

defn main():
  result =: sh('uname -a')
  out =: result.get(:out)
  lines =: out:lines
  say: "collected $(lines.#) line(s):"
  each line lines:
    say: "  $line"
