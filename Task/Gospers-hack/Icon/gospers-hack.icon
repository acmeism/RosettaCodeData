procedure main()
    every i := 1|3|7|15 do{
        writes(i,":")
        every writes(" ",gosperhack(i)\10)
        write()
    }
end

procedure gosperhack(n)
    n > 0 | fail
    repeat{
        a := iand(n,-n)
        b := n + a
        suspend n := ior(b, ishift(ixor(n,b),-2) / a)
    }
end
