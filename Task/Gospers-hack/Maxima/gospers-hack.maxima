load("bitwise")$

gosper(x) := block(
    c: bit_and(x, -x),
    r: x+c,
    bit_or(floor(bit_rsh(bit_xor(r, x), 2)/c), r)
)$

gosper10(x) := block(res: [], thru 10 do(x: gosper(x), push(x, res)), reverse(res))$

map(gosper10, [1, 3, 7, 15]);
