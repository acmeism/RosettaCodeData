create or replace function mygcd(a0, b0) as (
  with recursive cte as (
    select a0 as a, b0 as b
    union all
    select b as a, mod(a,b) as b
    from cte
    where b != 0
  )
  select last(a)
  from cte
  where b=0
);
