(let max (fun (L) {
  (mut index 0)
  (mut output nil)

  (while (< index (len L)) {
    (if (or (nil? output) (> (@ L index) output))
      (set output (@ L index)))
    (set index (+ 1 index)) })
  output }))

(print (max [1 2 3 0 -5 42 0 1]))
