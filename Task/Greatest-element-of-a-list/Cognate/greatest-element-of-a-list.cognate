Def Maximum as (
  Let L be input Of type (List?);
  When Empty? L (
    Error "Cannot take maximum of empty list."
  );
  Fold using (Max) with seed First L over Rest L
);

Print Maximum List (1 2 3 -4 98 2 2 65 17 2)
