int number ← random(1, 10 + 1)
int guess ← when(Runtime.args.length æ 1, number, -1)
while number ≠ guess
  guess ← ask(int, "Guess the number between 1 and 10 inclusive: ")
end
writeLine("Well guessed!")
