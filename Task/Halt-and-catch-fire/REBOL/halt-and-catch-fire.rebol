halt          ;; Stops evaluation and returns to the input prompt.
              ;; or
quit          ;; Stops evaluation and exits the interpreter.
              ;; or
quit/return 1 ;; with some status code
