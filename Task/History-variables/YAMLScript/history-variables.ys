!ys-0

defn main():
  exprs =: +['2 + 2' '6 * 7' '10 - 3']
  history =:
    for expr exprs:
      +{:expr expr :value eval(expr)}
  each item history:
    say: "$(item.expr) = $(item.value)"
  say: "last result: $(history.$.value)"
