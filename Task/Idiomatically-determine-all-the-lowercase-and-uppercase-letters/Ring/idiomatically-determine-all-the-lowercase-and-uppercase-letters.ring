# Project : Idiomatically determine all the lowercase and uppercase letters

aList = "a":"z"
see "Lower case : "
for n=1 to len(aList)
    see aList[n]
next
see nl
see "Upper case : "
aList = "A":"Z"
for n=1 to len(aList)
    see aList[n]
next
