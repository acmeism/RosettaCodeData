!ys-0

defn main():
  inf =: \\Inf
  ninf =: \\-Inf

  say: "+inf = $inf"
  say: "-inf = $ninf"

  big =: 1.7e308
  say: "$inf > $big -> $(inf > big)"
  say: "$ninf < -$big -> $(ninf < (- big))"

  say: "inf + 1 = $(inf + 1.0)"
  say: "inf + inf = $(inf + inf)"
  say: "inf - inf = $(inf - inf)"
