#import system.
#import system'io.

#symbol program =
[
    #var reader := "file.txt" file_path textreader.
    #loop (reader available)?
    [
       console writeLine:(reader read &literal).
    ].
].
