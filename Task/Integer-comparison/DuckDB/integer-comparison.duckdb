select a
  || if ( a < b, ' is less than ',
     if ( a = b, ' is equal to ',
     if ( a > b, ' is greater than ',
                 ' is incomparable to ')))
  || b as comparison
from read_csv_auto('/dev/stdin', header=false, sep=' ',
   columns= {'a': 'INTEGER', 'b': 'INTEGER'});
