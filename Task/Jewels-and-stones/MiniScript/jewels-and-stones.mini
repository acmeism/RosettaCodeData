import "stringUtil"

countJewels = function(s, j)
    count = 0
    for c in s
        if j.contains(c) then count += 1
    end for
    return count
end function

print countJewels("aAAbbbb", "aA")
print countJewels("ZZ", "z")
