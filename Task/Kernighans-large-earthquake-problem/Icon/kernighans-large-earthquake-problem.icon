procedure main(A)
   WS := ' \t'
   NWS := &cset -- WS
   /magnitude := \A[1] | 6
   every !&input ? {
       mag := (tab(upto(WS)),tab(many(WS)),tab(many(NWS)),tab(many(WS)),tab(0))
       if mag >= magnitude then write(&subject)
       }
end
