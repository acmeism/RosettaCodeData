!ys-0

defn main(n=10 k=4):
  say: sample(n k):joins

defn sample(n k):
  loop t 0, m 0, out []:
    cond:
      m == k: out
      rand-int(n - t) < (k - m):
        recur: t.++, m.++, out.conj(t)
      else:
        recur: t.++, m, out
