!ys-0

defn main(n=10):
  each i (1 .. n):
    say: (1 .. i).map(\(lah(i _))):joins

defn lah(n k):
  if k <= n:
    (binom(n.-- k.--) * factorial(n)) /: factorial(k)
    else: 0

defn binom(n k):
  factorial(n) //: factorial(k) * factorial(n - k)

defn factorial(n):
  reduce mul 1: 1 .. max(1 n)
