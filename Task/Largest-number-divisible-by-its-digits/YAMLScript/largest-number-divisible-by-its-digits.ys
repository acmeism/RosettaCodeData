!ys-0

defn main(limit=10000):
  say: (1 .. limit)
    .filter(divisible-by-digits?).$

defn divisible-by-digits?(n):
  digits =:
    map _ n:S:seq:
      fn(c): c:S:I
  every? _ digits:
    fn(d):
      if d == 0:
        false
        zero?(n % d)
