!ys-0

defn main(n=600851475143):
  say: largest-prime-factor(n)

defn largest-prime-factor(n):
  loop x n, d 2, last 1:
    if x != 1:
      if (x % d).!:
        recur: (x // d), d, d
        recur: x, d.++, last
      else: last
