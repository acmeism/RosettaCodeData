procedure main()
   every x := 1 to 100 do {
      writes(right(lpd(x),4))
      if x%10 = 0 then write()
      }
   write()
end

procedure lpd(n)
   every i := n/2 to 2 by -1 do return (n%i=0,i) | next
   return 1
end
