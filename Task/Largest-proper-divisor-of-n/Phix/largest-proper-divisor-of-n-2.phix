with javascript_semantics
for n=1 to 100 do
    integer hf = 1
    for p in get_primes_le(floor(sqrt(n))) do
        if remainder(n,p)=0 then
            hf = n/p
            exit
        end if
    end for
    printf(1,"%3d",hf)
    if remainder(n,10)=0 then puts(1,"\n") end if
end for
