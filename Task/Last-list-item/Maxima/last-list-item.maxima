cascade(list) := while length(list) > 1 do (
    m1: lmin(list), list: delete(m1, list, 1),
    m2: lmin(list), list: delete(m2, list, 1),
    list: endcons(m1+m2, list),
    print(list)
)$

cascade([6, 81, 243, 14, 25, 49, 123, 69, 11]);
