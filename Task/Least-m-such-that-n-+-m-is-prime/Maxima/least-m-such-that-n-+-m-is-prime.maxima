least_m(n) := block(fac: n!, next_prime(fac)-fac)$

makelist(least_m(i), i, 0, 49);

n: 50$
for i from 1 thru 9 do(
    while least_m(n) <= 1000*i do(n: n+1),
    print(sconcat("First m>", 1000*i, " is ", least_m(n), " for ", n, "!"))
)$
