for r in [range(0, 10), range(20, 110).by(10)] {
    for i in r {
        printf("!%d  = %s\n", i, left_fact(i));
    }
}

for i in range(1000, 10000).by(1000) {
    printf("!%d has %d digits.\n", i, left_fact(i).len);
}
