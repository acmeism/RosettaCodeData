int N, L, L0, L1, Add;
[Text(0, "Enter L(0), L(1), Add: ");
L0:= IntIn(0);
L1:= IntIn(0);
Add:= IntIn(0);
IntOut(0, L0);  ChOut(0, ^ );
IntOut(0, L1);  ChOut(0, ^ );
for N:= 3 to 25 do
        [L:= L1 + L0 + Add;
        IntOut(0, L);  ChOut(0, ^ );
        L0:= L1;
        L1:= L;
        ];
]
