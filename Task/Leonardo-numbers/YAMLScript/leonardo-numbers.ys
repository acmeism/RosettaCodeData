!ys-0

defn main(l0=1 l1=1 add=1 n=25):
  say: leonardo(l0 l1 add n):joins
  say: leonardo(0 1 0 25):joins

defn leonardo(l0 l1 add n):
  init =: +[l0 l1]
  loop i 2, a l0, b l1, acc init:
    if i < n:
      then:
        nx =: a + b + add
        recur: i.++ b nx acc.conj(nx)
      else: acc
