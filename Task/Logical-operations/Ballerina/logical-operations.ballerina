import ballerina/io;

function logic(boolean a, boolean b) {
    io:println("a and b: ", a && b);
    io:println("a or b:  ", a || b);
    io:println("not a:   ", !a);
}

public function main() {
    logic(true, false);
}
