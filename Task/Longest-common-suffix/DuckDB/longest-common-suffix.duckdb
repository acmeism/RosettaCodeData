create or replace function longest_common_suffix(lst) as (
  list_transform(lst, x->reverse(x)).longest_common_prefix().reverse()
);

## Example:
select longest_common_suffix(['aabc','xabc',  'axabc']  as lcs);
