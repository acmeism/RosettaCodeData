import extensions.

program =
[
    var a1 := ("a","b","c").
    var a2 := ("A","B","C").
    var a3 := (1,2,3).

    0 till(a1 length) do(:i)
    [
        console printLine(a1[i], a2[i], a3[i]).
    ].

    console readChar.
].
