#import system.

#symbol program =
[
    #var a1 := ("a","b","c").
    #var a2 := ("A","B","C").
    #var a3 := (1,2,3).

    #var i := Integer new:0.
    #loop (i < a1 length)?
    [
        console writeLine:(a1@i + a2@i + (a3@i) literal).
        i := i + 1.
    ].

    console readChar.
].
