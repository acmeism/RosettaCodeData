# Start at `start`
create or replace function task(start) as table (
   with recursive cte as (
     select start as n
     union all
     select
        n - 1 as n
     from cte
     where n > 0
  )
  from cte
);

from task(10);
