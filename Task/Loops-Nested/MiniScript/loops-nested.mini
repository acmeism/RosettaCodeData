import "fmt"

a = [null] * 20
for i in range(0, 19)
    a[i] = [0] * 20
    for j in range(0, 19)
        a[i][j] = floor(rnd * 21) + 1
    end for
end for

found = false
count = 0
for i in range(0, 19)
    for j in range(0, 19)
        print fmt.num(a[i][j], 4), ""
        if (count + 1) % 10 == 0 then print
        count += 1
        if a[i][j] == 20 then
            found = true
            break
        end if
    end for
    if found then break
end for
if count % 10 != 0 then print
