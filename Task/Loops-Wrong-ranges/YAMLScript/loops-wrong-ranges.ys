!ys-0

defn main():
  say: "1 .. 0 -> $(collect(1 .. 0):joins)"
  say: "0 .. -1 -> $(collect(0 .. -1):joins)"
  empty =: +[]
  say: "empty explicit -> $(collect(empty):joins)"

defn collect(xs):
  for i xs: i
