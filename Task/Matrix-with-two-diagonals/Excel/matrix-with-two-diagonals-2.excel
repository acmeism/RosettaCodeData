bothDiagonalMatrix
=LAMBDA(n,
    MAKEARRAY(
        n, n,
        LAMBDA(
            x, y,
            INT(OR(
                x = y,
                x = (1 + n - y)
            ))
        )
    )
)
