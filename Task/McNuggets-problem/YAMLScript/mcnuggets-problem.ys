!ys-0

defn main(limit=100):
  say:
    last:
      filter _ (0 .. limit):
        fn(n): mcnugget?(n).!

defn mcnugget?(n):
  some _ (0 .. (n / 6)):
    fn(x):
      some _ (0 .. (n / 9)):
        fn(y):
          left =: n - ((6 * x) + (9 * y))
          left >= 0 &&: (left % 20) == 0
