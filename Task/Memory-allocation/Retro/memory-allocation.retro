( display total memory available )
@memory putn

( display unused memory )
@memory here - putn

( display next free address )
here putn

( allocate 1000 cells )
1000 allot

( free 500 cells )
-500 allot
