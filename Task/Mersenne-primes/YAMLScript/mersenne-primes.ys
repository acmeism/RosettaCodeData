!ys-0

defn main(limit=31):
  each p (2 .. limit):
    m =: bit-shift-left(1 p)
    m =: m.--
    when prime?(p) && prime?(m):
      say: "M$p = $m"

defn prime?(n):
  condp gt n:
    2: false
    4: true
    else:
      every? _ (2 .. n:sqrt):
        fn(d): (n % d).?
