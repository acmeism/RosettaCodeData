!ys-0

defn main(limit=31):
  each p (2 .. limit):
    m =: bit-shift-left(1 p)
    m =: m.--
    when prime?(p) && prime?(m):
      say: "M$p = $m"

defn prime?(n):
  (n > 1) &&
  range(2 n:sqrt:I.++)
  .every?(\((n % _).?))
