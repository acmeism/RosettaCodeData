!ys-0

defn main(limit=20):
  each m (1 .. limit):
    say: "$m: $(minimum-multiple(m))"

defn minimum-multiple(m):
  loop k 1:
    n =: k * m
    if digit-sum(n) == m:
      n
      recur(k.++)

defn digit-sum(n):
  sum:
    map _ n:S:seq:
      fn(c): c:S:I
