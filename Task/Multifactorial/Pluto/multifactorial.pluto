local fmt = require "fmt"

local function mf(n, d)
    local prod = 1
    while n > 1 do
        prod *= n
        n -= d
    end
    return prod
end

for d = 1, 5 do
    fmt.write("degree %d: ", d)
    for n = 1, 10 do fmt.write("%8d", mf(n, d)) end
    print()
end
