!ys-0

defn main(n=12):
  # Header row
  print: '    |'
  each i (1 .. n):
    print: format(' %3d' i)
  say:
  say: "----+$('----' * n)"

  # Body: top half triangle only
  each i (1 .. n):
    print: format(' %2d |' i)
    each j (1 .. n):
      if j < i:
        print('    ')
        print(format(' %3d' (i * j)))
    say:
