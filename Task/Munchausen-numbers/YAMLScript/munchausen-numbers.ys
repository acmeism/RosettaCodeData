!ys-0

defn main(limit=5000):
  each n (1 .. limit):
    when n:munchausen?:
      say: n

defn munchausen?(n):
  digits =: n:str:split.map(N)
  digit-sum =: apply(+ digits.map(fn([d] (d ** d))))
  n ==: digit-sum
