create or replace function ngrams(str, len) as table (
    with ng as (from ngrams_table(str, len))
    select count(*) as count, gram
    from ng group by gram
    order by count desc, gram asc
  );

select count, gram as "4-gram" from ngrams('Live and let live', 4);
