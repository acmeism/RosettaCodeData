# The histogram of the n-grams of upper(str) as a STRUCT
create or replace function ngrams(str, len) as (
  select histogram(gram) from ngrams_table(str,len)
);

select n, ngrams('Live and let live', n) from range(2,5) t(n);
