# Find all n where sum of digits, each raised to len, is n.
# Trick: build ascending digit numbers, find sum, back-match to an n.

Digits   ← ≡₀°⋕⇡10
NextMag  ← ▽⊸≡◇(≍⟜⍆)♭⊞⍚⊂Digits    # Add next digit to all.
PowerSum ← °⋕/+ⁿ⊃(⧻|≡₀⋕)          # Sum digits^len
ShowNarc ← □⍥(⋅"")⤚(¬≍⍆)⊸PowerSum # Return an N or space.
Tidy     ← ⍆▽⊸≡◇(≠0⧻)             # Remove blanks, sort.
⍥(⊃(∘|˜⊂ Tidy ≡◇ShowNarc) NextMag)7 ⟜∘{""}
↙25≡⋕↘1◌
