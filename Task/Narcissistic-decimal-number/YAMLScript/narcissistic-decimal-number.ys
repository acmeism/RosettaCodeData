!ys-0

defn main(count=20):
  narcs =:
    filter narcissistic?:
      iterate inc: 0
  say: take(count narcs):joins

defn digits(n):
  loop n n, ds []:
    if n == 0:
      then: ds
      else: recur(quot(n 10) ds.conj(n % 10))

defn narcissistic?(n):
  raw =: digits(n)
  ds =:
    if raw:empty?:
      then: +[0]
      else: raw
  m =: ds.#
  psum =:
    reduce _ 0 ds:
      fn(acc d):
        add: acc pow(d m)
  n ==: psum
