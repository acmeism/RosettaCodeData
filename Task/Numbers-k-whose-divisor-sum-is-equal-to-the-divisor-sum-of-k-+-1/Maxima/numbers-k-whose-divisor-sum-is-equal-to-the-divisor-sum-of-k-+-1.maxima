block(
    n: 1, count: 0, while count < 50 do(
        if divsum(n) = divsum(n+1) then block(
            sprint(n),
            count: count+1,
            if mod(count, 5) = 0 then newline()
        ),
        n: n+1
    )
)$
