!ys-0

PRIME-DIGITS =: '2357'

defn main(limit=1000):
  each n (1 .. limit):
    ds =: n:S
    prime-digits? =: every?(\(in?(_ PRIME-DIGITS)) ds)
    sum-13? =: n:digits:sum == 13
    when prime-digits? && sum-13?:
      say: n
