include "cowgol.coh";

var n: uint16 := 10;
loop
    var sq := n * n;
    if sq >= 1000 then break; end if;
    if sq % 2 == 1 then
        print_i16(sq);
        print_nl();
    end if;
    n := n+1;
end loop;
