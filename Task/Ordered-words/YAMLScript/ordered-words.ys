!ys-0

defn main(path='/usr/share/dict/words'):
  words =: read(path):lines.map(trim).filter(ordered?)
  longest =: apply(max words.map(len))
  ordered =: words.filter(\(_.# == longest))
  each w ordered: !say w

defn ordered?(w):
  nums =: w.map(int)
  le: nums*
