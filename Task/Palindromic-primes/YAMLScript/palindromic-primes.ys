!ys-0

defn main(limit=1000):
  each n (2 .. limit):
    when prime?(n) && palindrome?(n:S):
      say: n

defn palindrome?(s):
  s == s:reverse

defn prime?(n):
  (n > 1) &&
  range(2 n:sqrt:I.++)
  .every?(\((n % _).?))
