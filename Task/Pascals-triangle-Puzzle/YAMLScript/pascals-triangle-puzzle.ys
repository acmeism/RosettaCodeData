!ys-0

defn main(rows=10):
  each row triangle(rows):
    say: row:joins

defn triangle(rows):
  loop n 1, row [1], out []:
    if n <= rows:
      recur: n.++, next-row(row), out.conj(row)
      else: out

defn next-row(row):
  +[1] + mapv(add row drop(1 row)) + [1]
