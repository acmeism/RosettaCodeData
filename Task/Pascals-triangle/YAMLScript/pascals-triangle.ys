!ys-0

defn main(n=6):
  loop row [1], i 1:
    say: row.join(' ')
    when i < n:
      recur: next-row(row) i.++

defn next-row(row):
  padded =: ([0] + row + [0])
  padded: .partition(2 1)
          .map(\(_.0 + _.1))
