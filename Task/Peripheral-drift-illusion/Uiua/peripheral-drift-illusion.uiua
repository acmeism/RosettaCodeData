# Experimental!
# ^ needed for ⍉₂
Blu   ← 0.2_0.4_0.45
Yello ← 0.8_0.8_0.4

# Add the box onto the image. The stack is a mess :-)
Add ← (
  ⊙(⊃⋅∘⊙∘)   # stack->bab
  ⟜(≡₁+⊙°⊡)  # add the offset to the co-ords
  ⍥⊙⍉₂<°⊟↘¯1 # transpose based on location
  ⍥⊙⇌<0.5⚂   # flip 50% of the time
  # `pick` args --> coords target newvalues
  ⍜⊡⋅∘⊃⊙⋅∘⋅∘ # stack->acb
)
˜↯Blu ⇌⊂3↯2 16        # base square
Rep ← ⍜(⊏0_1)≡×⊃⋅∘∘   # replaces an edge with multiple
⍜⍉₂(Rep2)Rep2         # brighten two edges
⍜⍉₂(Rep0.5)Rep0.5⍜⇌⍉₂ # darken two edges
˜↯Yello ⇌⊂3↯2 400     # draw the background
⊙◌+25×26°⊡˜↯0 ↯2 14   # calculate offsets
⊙◌∧(Add˜⊂0)♭₂         # Add boxes at these offsets
