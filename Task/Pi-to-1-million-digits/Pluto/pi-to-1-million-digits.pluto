require "bignum"
local fmt = require "fmt"

mpfr.init(3_322_000, 1_000_000)  -- use MPFR
mpfr.convert(false)
local start = os.clock()
local pi = mpfr.pi()
print(fmt.abridge(pi, 20))
print($"\nTook {os.clock() - start} seconds")
