local int = require "int"
local fmt = require "fmt"

print("Decimal numbers under 1,000 whose digits include two 1's:")
local results = range(11, 911):filter(|i| -> int.digits(i):countvalues()[1] == 2):reorder()
fmt.tprint("%5d", results, 7)
print($"\nFound {#results} such numbers.")
