isDivisible :: (int, int) -> bool
isDivisible n d =
  if (d * d > n) then false
  else if (n % d == 0) then true
  else isDivisible(n, d + 1)

isPrime :: int -> bool
isPrime n =
  if (n < 2) then false
  else if (isDivisible(n, 2)) then false
  else true
