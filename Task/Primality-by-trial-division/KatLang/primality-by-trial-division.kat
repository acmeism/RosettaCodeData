open Math

IsPrime(1) = 0
IsPrime(2) = 1
IsPrime(3) = 1
IsPrime(N) = {
    IsPrimeStep = D+2, N mod D != 0 and D <= Sqrt(N)
    Output = if(
        N mod 2 == 0,
        0,
        N mod while(IsPrimeStep, 3):0
    )
}

range(1,100).filter(IsPrime)
