val .isPrime = f(.i) {
    val .n = abs(.i)
    if .n <= 2: return .n == 2

    val .chkdiv = f(.n, .i) {
        if .i x .i <= .n {
            return .n ndiv .i and self(.n, .i+2)
        }
        return true
    }

    return .n ndiv 2 and .chkdiv(.n, 3)
}

writeln filter .isPrime, series 100
