function isprime(n)
    if n<2 then return false end
    for i=2,math.sqrt(n) do
        if n%i==0 do return false end
    end
    return true
end

for i = 1,20 do
    if isprime(i) then
        io.write(i,'\t')
    end
end
