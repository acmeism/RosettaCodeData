!ys-0

defn main(n=12):
  say: factors(n):joins

defn factors(n):
  loop n n, d 2N, acc []:
    if n > 1:
      if (n % d):zero?:
        recur (n / d) d: acc.conj(d)
        recur n d.++: acc
      else: acc
