local int = require "int"
local fmt = require "fmt"
require "table2"

print("The products of positive divisors for the first 50 positive integers are:")
for i = 1, 50 do
    fmt.write("%9d  ", int.divisors(i):prod())
    if i % 5 == 0 then print() end
end
