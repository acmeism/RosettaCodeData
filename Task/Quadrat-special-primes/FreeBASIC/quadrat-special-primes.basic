#include "isprime.bas"

dim as integer p = 2, j = 1
print 2;" ";
do
    do
        if isprime(p + j*j) then exit do
         j += 1
    loop
    p += j*j
    if p > 16000 then exit do
    print p;" ";
    j = 1
loop
print
