extern crate debug;
fn main() {
    let x = "extern crate debug;\nfn main() {\n let x = ";
    let y = "print!(\"{}{:?};\n let y = {:?};\n {}\", x, x, y, y)\n}";
    print!("{}{:?};
    let y = {:?};
    {}", x, x, y, y)
}
