Latin ← (
  ≡↻⇡⊸⧻↯⊃(↯2|⇡)                 # Generate base square
  S ← ⍥(⍜(⊏|⇌)≡(⌊×⚂)↯2⊸⧻)⊸(-1⧻) # Shuffle by swapping rows
  S⍉S                           # Shuffle, tranpose, shuffle
)
⍥(&sLatin5)2
