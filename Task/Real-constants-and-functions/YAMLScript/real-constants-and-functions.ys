!ys-0

defn main():
  say: "e        = $(Math/E)"
  say: "pi       = $(Math/PI)"
  say: "sqrt(2)  = $(Math/sqrt(2))"
  say: "log(e)   = $(Math/log(Math/E))"
  say: "exp(1)   = $(Math/exp(1))"
  say: "abs(-3)  = $(Math/abs(-3))"
  say: "floor    = $(Math/floor(3.7))"
  say: "ceil     = $(Math/ceil(3.2))"
  say: "pow(2,8) = $(Math/pow(2 8))"
