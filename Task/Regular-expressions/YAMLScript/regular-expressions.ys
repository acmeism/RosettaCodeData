!ys-0

defn main(s='I am a string'):
  # Match: check if the string contains 'string'
  if s =~ /string/:
    say: 'matched'
    say: 'no match'
  # Substitute: replace 'string' with 'replacement'
  say: s.replace(/string/ 'replacement')
