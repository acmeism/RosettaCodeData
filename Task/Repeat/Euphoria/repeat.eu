procedure repeat(integer proc, integer times)
	for i = 1 to times do
		call_proc(proc, {})
	end for
end procedure

procedure say_hello()
	puts(1, "hello\n")
end procedure

repeat(routine_id("say_hello"), 3)
