create or replace table test(n HUGEINT, i INTEGER);

insert into test values
   (7,3),
   (1,0),
   (123456789123456789, 23);

select n, i, divmod(n, i) as divmod, (divmod[1] * i + divmod[2]) = n
from test;
