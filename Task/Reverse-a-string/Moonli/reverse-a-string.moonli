defun string-reverse(string):
  let len = length(string),
      reverse = copy-seq(string):
    loop :for i :below len :do
      aref(reverse, i) = aref(string, len - i - 1)
    end
    reverse
  end
end
