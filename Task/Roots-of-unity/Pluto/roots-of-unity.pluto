require "complex"

local function roots(n)
    local r = {}
    for i = 1, n do r[i] = complex.polar(1, 2 * math.pi * (i - 1) / n) end
    return r
end

for n = 2, 5 do
    print($"{n} roots of 1:")
    for roots(n) as r do print(r:format("  % 0.14f", "%0.14f")) end
end
