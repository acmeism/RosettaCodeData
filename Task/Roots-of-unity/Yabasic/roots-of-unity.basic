twopi = 2 * pi
n = 5

for m = 0 to n-1
    theta = m*twopi/n
    real = cos(theta)
    imag = sin(theta)
    if imag >= 0 then
        print real using("##.########"), " + ", imag using("#.########"), "i"
    else
        print real using("##.########"), " + ", -imag using("#.########"), "i"
    end if
next m
