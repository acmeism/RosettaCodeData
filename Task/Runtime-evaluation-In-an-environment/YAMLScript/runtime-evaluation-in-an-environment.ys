!ys-0

defn main(expr='(+ x (* y 2))'):
  x =: 3
  y =: 7
  env =: +{symbol('x') x, symbol('y') y}
  say: "$expr where x=$x, y=$y => $eval-in-env(expr env)"

defn eval-in-env(expr env):
  x =: env.get(symbol('x'))
  y =: env.get(symbol('y'))
  eval: read-string("(let [x $x y $y] $expr)")
