primorial(n) := block(p: 2, res: 1, thru n do(res: p*res, p: next_prime(p)), res)$

block(count: 0, n: 1, while count < 20 do(
        pn: primorial(n),
        if primep(pn+1) or primep(pn-1) then block(
            sprint(n),
            count: count+1
        ),
        n: n+1
    )
)$
