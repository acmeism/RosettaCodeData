block(count: 1, n: 1, while count < 16 do(
        if cardinality(divisors(n)) = count then block(
            sprint(n),
            count: count+1,
            n: 1
        ),
        n: n+1
    )
)$
