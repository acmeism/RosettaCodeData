!ys-0

defn main(limit=12):
  each n (1 .. limit):
    say: "$n: $(smallest-with-divisors(n))"

defn smallest-with-divisors(want):
  loop n 1:
    if divisor-count(n) == want:
      n
      recur(n.++)

defn divisor-count(n):
  count:
    filter _ (1 .. n):
      fn(d):
        (n % d):zero?
