x: read("Enter any real number:")$

iters: 0$

while true do (
x_new: 0.86*(x+3),
delta: abs(x_new-x),
iters: iters+1,
if delta < 10^(-15) then return(sconcat(x_new, " (", iters, " iterations before convergence)")),
x: x_new);
