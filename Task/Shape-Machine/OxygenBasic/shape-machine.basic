uses console

double prev, sgte
int cnt

cnt = 1
input() prev

do
    sgte = (prev + 3) * 0.86
    if prev = sgte then exit do
    printl sgte
    prev = sgte
    cnt ++
loop

printl cr "Took " cnt " iterations."

waitkey
