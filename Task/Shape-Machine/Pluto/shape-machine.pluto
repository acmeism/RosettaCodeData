require "io2"

local prev = io.readNum("")
local count = 1
while true do
    local next = (prev + 3) * 0.86
    if prev == next then break end
    prev = next
    print(prev)
    count += 1
end
print($"Took {count} iterations.")
