LET cnt = 1
INPUT prev

LET sgte = (prev + 3) * .86
DO WHILE prev <> sgte
   PRINT sgte
   LET prev = sgte
   LET cnt = cnt + 1
   LET sgte = (prev + 3) * .86
LOOP
PRINT "Took "; cnt; " iterations."
END
