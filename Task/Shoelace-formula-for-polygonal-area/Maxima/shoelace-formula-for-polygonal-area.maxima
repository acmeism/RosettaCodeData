shoelace(lst) := block(
    x: map(lambda([l], l[1]), lst),
    y: map(lambda([l], l[2]), lst),
    left: apply("+", x*endcons(first(y), rest(y))),
    right: apply("+", y*endcons(first(x), rest(x))),
    abs(left-right)/2
)$

shoelace([[3, 4], [5, 11], [12, 8], [9, 5], [5, 6]]);
