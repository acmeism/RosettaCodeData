#!/usr/bin/env ys-0

defn main():
  each row (0 .. 15):
    line =:
      for col (0 .. 5):
        format-cell: (32 + (col * 16)) + row
    say: join('   ' line)

defn format-cell(n):
  format('%3d - %-3s' n label(n))

defn label(n):
  case n:
    32: 'Spc'
    127: 'Del'
    else: char(n):S
