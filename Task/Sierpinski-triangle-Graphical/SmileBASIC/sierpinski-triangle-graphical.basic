OPTION STRICT
OPTION DEFINT
DEF DRAW X0, Y0, DEPTH
  VAR X, Y, SIZE
  SIZE = 1 << DEPTH
  FOR Y = 0 TO SIZE - 1
    FOR X = 0 TO SIZE - 1
      IF (X AND Y) == 0 THEN
        GPSET X0 + X, Y0 + Y, RGB(X, 255 - Y, 255)
      ENDIF
    NEXT
  NEXT
END
CALL "DRAW", 96, 32, 7
END
