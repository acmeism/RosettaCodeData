#!/usr/bin/env ys-0

defn main(n=4):
  size =: 2 ** n
  each i (0 .. size.--):
    say: row(i size)

defn row(i size):
  pad =: (size - i) * ' '
  cells =:
    for j (0 .. i):
      if bit-and(i j) == j: +'* ' '  '
  str pad: cells.apply(str)
