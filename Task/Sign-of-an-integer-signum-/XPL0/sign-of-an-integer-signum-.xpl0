func SigNum(X);
int  X;
return if X>0 then 1 else if X<0 then -1 else 0;

int  N, I;
[N:= [-10, 42, 0, -0, $8000_0000];
for I:= 0 to 4 do
    [IntOut(0, N(I));  ChOut(0, 9\tab\);
     IntOut(0, SigNum(N(I)));  CrLf(0);
    ];
]
