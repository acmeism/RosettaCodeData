(de deldestr (Item "Var")
   (let Lst (val "Var")
      (let? M (member Item Lst)
         (ifn (prior M Lst)
            (set "Var" (cdr Lst))
            (con @ (cdr M))
            (val "Var") ) ) ) )
(let (N 3  L (range 1 5))
   (println 'L L)
   (deldestr N 'L)
   (println 'fin 'L L) )
