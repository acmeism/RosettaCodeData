!ys-0

defn main(limit=49):
  each n (1 .. limit):
    say: "$n: $(smallest-square-starting(n))"

defn smallest-square-starting(n):
  prefix =: n:S
  loop k 1:
    sq =: k:sqr
    if sq:S.starts?(prefix):
      sq
      recur(k.++)
