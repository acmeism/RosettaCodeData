digsum4(n) := sum(mod(floor(n/10^i), 10), i, 0, 3)$

ifactors_full(n) := flatten(map(lambda([l], apply(makelist, l)), ifactors(n)))$

smithp(n) := not primep(n) and digsum4(n) = lsum(i, i, map(digsum4, ifactors_full(n)))$

sublist(makelist(i, i, 1, 9999), smithp);
