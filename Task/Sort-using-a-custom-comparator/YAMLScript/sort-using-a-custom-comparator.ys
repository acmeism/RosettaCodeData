!ys-0

defn main(*):
  words =: qw(
    banana apple cherry date elderberry fig grape)
  say: words.sort(by-length-then-alpha _):joins

defn by-length-then-alpha(a b):
  if a.# == b.#:
    then: a:lc.compare(b:lc)
    else: b.# - a.#
