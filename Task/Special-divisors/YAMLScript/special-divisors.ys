!ys-0

defn main(limit=100):
  each n (1 .. limit):
    ds =: special-divisors(n)
    when ds:
      say: "$n: $(ds:joins)"

defn special-divisors(n):
  for d (1 .. n):
    when (n % d).!:
      q =: n // d
      when gcd(d q) == 1:
        d

defn gcd(a b):
  if b.!:
    abs: a
    gcd: b (a % b)
