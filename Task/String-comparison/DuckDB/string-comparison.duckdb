select s,
       t,
       s == t,
       s != t,
       s < t,
       s > t,
       upper(s) = upper(uppers) as "case insenstive",
       '2' < '1e10',
       s ilike uppers
from (select 'abc' as s, 'def' as t, 'ABC' as uppers );
