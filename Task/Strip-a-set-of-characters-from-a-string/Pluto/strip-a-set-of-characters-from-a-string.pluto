require "uchar"

local function strip_characters(s, t)
    local u = uchar.of(s)
    for i = u:len(), 1, -1 do
        if u:get(i) in t then u:remove(i) end
    end
    return u:tostring()
end

print(strip_characters("She was a soul stripper. She took my heart!", "aei"))
