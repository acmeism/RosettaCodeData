!ys-0

dflt =: |-
  apples, pears # and bananas
  apples, pears ; and bananas

defn main(text=dflt):
  each line text:lines:
    say: line:strip-comment

defn strip-comment(line):
  line.replace(/[#;].*$/).trim()
