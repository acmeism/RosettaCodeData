!ys-0

defn main(n=1234, base=10):
  sum =: n.sum-digits(base)
  say: "Sum of digits of $n in base $base: $sum"

defn sum-digits(n base):
  digits =:
    Long/toString(n base)
      .map(\(Long/parseLong(_:S base)))
  sum: digits
