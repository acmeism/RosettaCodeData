Const pi = 4 * Atn(1)

Print "s(1000) = "; sumSeries#(1000)
Print "zeta(2) = "; pi * pi / 6
End

Function sumSeries# (n)
    Dim sum As Double
    If n = 0 Then sumSeries# = 0
    sum = 0
    For k = 1 To n
        sum = sum + 1.0 / (k * k)
    Next
    sumSeries# = sum
End Function
