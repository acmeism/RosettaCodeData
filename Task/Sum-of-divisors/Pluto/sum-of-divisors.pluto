local int = require "int"
local fmt = require "fmt"
local sd = range(1, 100):map(|i| -> int.divstat(i)[2])
print("The sums of positive divisors for the first 100 positive integers are:")
fmt.tprint("%3d ", sd, 10)
