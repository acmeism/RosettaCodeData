scope # show the sums of the cubes upto n for 0 <= n < 50
    local cubeSum := 0;
    for n from 0 to 49 do
        cubeSum +:= n * n * n;
        printf( "%8d", cubeSum );
        if n mod 10 = 9 then print() fi
    od
end
