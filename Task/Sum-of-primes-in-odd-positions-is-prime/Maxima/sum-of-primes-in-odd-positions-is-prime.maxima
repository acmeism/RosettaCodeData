primeseq: primes(2, 1000)$
primesum: 0$
for i from 1 thru length(primeseq) step 2 do(
    p: primeseq[i],
    primesum: primesum+p,
    if primep(primesum) then block(
        sprint("i =", i, "p(i) =", p, "sum =", primesum),
        newline()
    )
)$
