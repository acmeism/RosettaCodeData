digsum(n, nd) := sum(mod(floor(n/10^i), 10), i, 0, nd-1)$

prime_dps(n, pow, maxd) := primep(digsum(n^pow, maxd))$

sublist(makelist(i, i, 1, 99), lambda([n], prime_dps(n, 2, 4) and prime_dps(n, 3, 6)));
