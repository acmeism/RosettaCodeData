local int = require "int"
local fmt = require "fmt"

local res = {}
for n = 1, 99 do
    local ds2 = int.digstat(n * n)[2]
    local ds3 = int.digstat(n * n * n)[2]
    if int.isprime(ds2) and int.isprime(ds3) then res:insert(n) end
end
fmt.lprint(res)
