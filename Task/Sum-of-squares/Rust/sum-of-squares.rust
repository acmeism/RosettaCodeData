fn sqsum(v: Vec<f64>) -> f64 {
    let mut s = 0.0;

    for i in v.iter() {
        s += *i * *i;
    }

    return s;
}

fn main() {
    let v = vec!(3.0, 1.0, 4.0, 1.0, 5.0, 9.0);
    println!("{}", sqsum(v));

    let u : Vec<f64> = vec!();
    println!("{}", sqsum(u));
}
