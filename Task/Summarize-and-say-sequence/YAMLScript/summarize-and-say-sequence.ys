!ys-0

defn main(start='1', count=8):
  loop s start, n count:
    when n > 0:
      say: s
      recur: summarize(s), n.--

defn summarize(s):
  parts =:
    partition-by a: s
  join:
    for p parts:
      "$(p:count)$(p.0)"
