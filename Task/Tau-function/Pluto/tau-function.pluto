local int = require "int"
local fmt = require "fmt"

local limit <const> = 100
local tau = {}
local i = 0
print($"The tau functions for the first {limit} positive integers are:")
for i = 1, limit do
    tau:insert(int.divstat(i)[1])
end
fmt.tprint("%2d ", tau, 10)
