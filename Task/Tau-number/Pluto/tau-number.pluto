local int = require "int"
local fmt = require "fmt"

local limit <const> = 100
local tau = {}
local i = 0
print($"The first {limit} tau numbers are:")
repeat
    ++i
    local dc = int.divstat(i)[1]
    if i % dc == 0 then tau:insert(i) end
until #tau == limit
fmt.tprint("%4d ", tau, 10)
