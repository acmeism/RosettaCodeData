!ys-0

defn main(n=100):
  each x take(n filter(tau-number? (1 .. \\Inf))):
    say: x

defn tau-number?(n):
  (n % divisor-count(n)) == 0

defn divisor-count(n):
  (1 .. n).filter(\((n % _) == 0)).#
