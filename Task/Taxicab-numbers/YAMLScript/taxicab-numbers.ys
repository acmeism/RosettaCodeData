!ys-0

defn main(limit=100):
  cubes =: (1 .. limit).map(\([_ cube(_)]))
  sums =:
    frequencies:
      for a cubes, b cubes :when (a.0 <= b.0):
        a.1 + b.1
  winners =: sums:seq.filter(\(_:val > 1)).map(key):sort
  each n winners.take(6):
    say: n
