ENUMFROMTHENTO
=LAMBDA(a,
    LAMBDA(b,
        LAMBDA(z,
            LET(
                d, b - a,

                SEQUENCE(
                    1 + FLOOR.MATH((z - a)/d),
                    1, a, d
                )
            )
        )
    )
)
