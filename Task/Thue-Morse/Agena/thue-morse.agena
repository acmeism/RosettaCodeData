scope # Thue-Morse sequence

    local proc thue_morse( n :: integer ) :: string
        local sb0, sb1 := "0", "1";
        to n do sb0, sb1 := sb0 & sb1, sb1 & sb0 od;
        return sb0
    end;

    for i from 0 to 6 do printf( "%d : %s\n", i, thue_morse( i ) ) od

epocs
