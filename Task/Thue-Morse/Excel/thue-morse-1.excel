thueMorse
=LAMBDA(n,
    APPLYN(n)(
        LAMBDA(bits,
            APPENDCOLS(bits)(
                LAMBDA(x,
                    IF(0 < x, 0, 1)
                )(bits)
            )
        )
    )(0)
)
