(do ;;; Thue-Morse sequence

    (fn thue-morse [n]
        (var (sb0 sb1) (values "0" "1"))
        (for [_ 1 n]
             (set (sb0 sb1) (values (.. sb0 sb1) (.. sb1 sb0)))
        )
        sb0
    )

    (for [i 0 6]
         (print (.. i " : " (thue-morse i)))
    )

)
