procedure main(A)
   limit := \A[1] | 5
   i := 0
   every (s := tm()\limit,write("TM",1(i,i +:= 1),": ",s))
end

procedure tm()  # Infinite sequece of TM sequences
   suspend (s := "0") | |(s ||:= map(s,"01","10"))
end
