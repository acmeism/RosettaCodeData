!ys-0

defn main(n=6):
  seq =: n:thue-morse
  say: seq:joins

defn thue-morse(n):
  init =: +[0]
  loop seq init, i 0:
    if i >= n:
      then: seq
      else:
        next-seq =: seq.concat(seq.map(\(1 - _)))
        recur: next-seq i.++
