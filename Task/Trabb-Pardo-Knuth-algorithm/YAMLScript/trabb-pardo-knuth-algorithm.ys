!ys-0

defn main(*):
  say: "Enter 11 numbers:"
  S =:
    for [_ (1 .. 11)]:
      N(trim(read-line()))
  each x reverse(S):
    result =: f(x)
    if result > 400:
      say: "$x -> OVERFLOW"
      say: "$x -> $result"

defn f(x): sqrt(abs(x)) + (5 * x:cube)
