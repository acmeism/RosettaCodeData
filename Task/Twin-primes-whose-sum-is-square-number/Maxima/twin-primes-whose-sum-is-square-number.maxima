is_twin(p) := primep(p+2)$
twins: sublist(primes(1, 10000000), is_twin)$
sum_is_square(p) := integerp(sqrt(2*(p+1)))$
results: sublist(twins, sum_is_square)$
for p in results do(print(sconcat(p, " + ", p+2, " = ", 2*(p+1), " = ", sqrt(2*(p+1)), "^2")));
