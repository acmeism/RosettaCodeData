local fmt = require "fmt"

local i = 1
while true do
    local b2 = fmt.itoa(i, 2)
    b2 ..= b2
    local d = tonumber(b2, 2)
    if d >= 1000 then break end
    fmt.print("%3d : %s", d, b2)
    i += 1
end
print($"\nFound {i - 1} numbers.")
