!ys-0

defn main(n=-42 bits=8):
  value =: twos(n bits)
  say: "$n in $bits bits = $(binary(value bits))"

defn twos(n bits):
  mask =: bit-shift-left(1 bits).--
  bit-and: n mask

defn binary(n bits):
  s =: Long/toBinaryString(n)
  ((bits - s.#) * '0') +: s
