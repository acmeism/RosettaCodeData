!ys-0

# URL-encode a string: every byte outside [0-9A-Za-z] becomes %HH.

defn main(s='http://foo bar/'):
  say: s:encode

defn- encode(s):
  join: s.map(enc-char)

defn- enc-char(c):
  if c =~ /[0-9A-Za-z]/:
    str: c
    format '%%%02X': int(c)
