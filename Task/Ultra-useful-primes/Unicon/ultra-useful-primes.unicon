link fastprime

procedure main(A)
   limit := \A[1] | 10
   cnt := 0
   every n := seq() do {
      every (x := 2^2^n, k := seq(1,2)) do
         if (is_prime(x-k), cnt +:= 1) then (write(" a(",n,") = ",k),break)
      if cnt >= limit then break
      }
end
