create or replace table t as from read_csv('unescape.txt', headers=false,
  columns={'line': VARCHAR}, sep='');

select line, from_json(try_cast('"' || line || '"' as JSON), '"VARCHAR"') as varchar from t;
