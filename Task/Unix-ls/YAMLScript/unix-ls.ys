!ys-0

# List the entries of a directory, sorted, one per line.
# fs/ls yields "./name" paths; basename strips the leading "./".

defn main(dir='.'):
  each name fs/ls(dir).map(fs/basename):sort:
    say: name
