local function variadic(...args)
    for args as arg do print(arg) end
end

variadic("Pip", "Squeak", "Wilfred")
local a = {}
a:insert("Faith")
a:insert("Hope")
a:insert("Charity")
variadic(a:unpack())
