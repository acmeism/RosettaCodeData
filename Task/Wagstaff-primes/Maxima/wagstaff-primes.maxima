block(
    p: 3, count: 0, while count < 10 do(
        m: (2^p+1)/3,
        if primep(m) then block(
            sprint("p =", p, "m =", m),
            newline(),
            count: count+1
        ),
        p: next_prime(p)
    )
)$
