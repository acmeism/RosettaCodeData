wilson(p, n) := if p>n then (n-1)!*(p-n)!-(-1)^n else (-1)^(n+1)$

is_wilson(p, n) := integerp(wilson(p, n)/p^2)$

wilsons(n) := sublist(primes(2, 11000), lambda([p], is_wilson(p, n)))$

makelist(wilsons(i), i, 1, 11);
