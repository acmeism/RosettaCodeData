Words ← ▽⊸(≥3≡◇⧻)⊜□⊸≠@\n&fras"RosettaCode/unixdict.txt"
#
KeepNice ← ▽⊸(↧⊃(≥0|≤26)≡◇(-@a⊢))▽⊸(≤9≡◇⧻)▽⊸(≥3≡◇⧻)
Words    ← KeepNice⊜□⊸≠@\n&fras"RosettaCode/unixdict.txt"

Alpha    ← "abcdefghijklmnopqrstuvwxyz"
Mask     ← /+≡˜⌕¤Alpha                 # Mask against alphabet.
Masks    ← ≡◇(/+≡⌕)Words¤¤Alpha        # Masks for all words.
CanMake  ← /↧≥0-Mask                   # Does each letter in cand appear <= each letter in target?
HaveCore ← ▽⊸≡◇˜∊¤⊢                    # Keep cands which have the central letter.
Matches  ← ˜⊏Words⊚≡(/↧≥0-)Masks ¤Mask # Words that be spelt using target.
Answers  ← ⊏⍖⊸≡◇⧻HaveCore⟜Matches      # Keep cands which lead with core letter.
&p$"Part 1: _" Answers "kndeogelw"

Words₉ ← ▽⊸≡◇(=9⧻)Words
# Find all matches for a word, check which letters are used and
# their counts, choose the best -> [count best_letter]
CountFor ← ⟜˜⨂⊸/↥/+≡◇(>0Mask)Matches

˜⊡Words₉⟜⊡˜⨂⊸(/↥)⊸≡⊢⍉⊟≡◇CountFor Words₉
&p$"Part 2 - best word '_': _ matches with '_' at centre" °□⊙⊙(˜⊡Alpha)⊙°⊟
&p$"Confirmation - _ answers: _" ⊸⧻ Answers "etrminals"
