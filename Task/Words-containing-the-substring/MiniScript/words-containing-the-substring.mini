// find 12+ character words in unixdict.txt that contain "the"

inf = file.open( "unixdict.txt", "r" )

line = inf.readLine
while not inf.atEnd
   if line.len > 11 then
      if line.indexOf( "the" ) >= 0 then
         print line
      end if
   end if
   line = inf.readLine
end while

inf.close
