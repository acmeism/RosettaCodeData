!ys-0

defn main(substr='the'):
  each word filter(containing?(substr) words()):
    say: word

defn containing?(substr):
  fn(word):
    if word.# > 11:
      substr.in?(word:lc)
      false

defn words():
  file =: '/usr/share/dict/words'
  if fs/e(file):
    file:read:lines
    qw(thermodynamic nevertheless neverthelessy thereabouts
       antithesis synthetic theoretical)
