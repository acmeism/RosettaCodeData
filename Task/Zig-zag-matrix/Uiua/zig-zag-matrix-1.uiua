ZigZag ← (
  ↘₁⇡⊸×₂
  # (row offsets|col offsets|parity for reversals|digits grouped)
  ⊃(⋅⟜⇡|↧₀-|◿₂|+⊃⍚⇡(\+⊂0↘₋₁)↧⇌.)
  # (trim|rot rows|rot cols|alternately reverse digit groups, fill)
  ⊙(≡↙|≡↻|⍉≡↻|⬚∞≡◇⍥⇌)
)

ZigZag 5
